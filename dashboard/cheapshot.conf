# CheapShot Dashboard NGINX Configuration
# Copy to: /home/ubuntu/docker-stuff/nginx-config/cheapshot.skmredacted.com.conf

server {
    listen 80;
    server_name cheapshot.skmredacted.com;
    
    # Cloudflare Tunnel handles SSL termination externally
    # This server receives HTTP from cloudflared
    
    # Static files - Dashboard frontend
    root /home/ubuntu/github-repos/CheapShot/dashboard/public;
    index index.html;
    
    # API proxy to Express backend on port 4847
    # Use 172.17.0.1 to reach host from Docker container
    location /api/ {
        proxy_pass http://172.17.0.1:4847;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        
        # WebSocket support (if needed in future)
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
    
    # OAuth callback redirect (important for Discord OAuth)
    location /callback {
        try_files $uri /index.html;
    }
    
    # SPA routing - serve index.html for unmatched routes
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # Cache static assets
    location ~* \.(css|js|png|jpg|jpeg|gif|ico|svg|woff|woff2)$ {
        expires 1d;
        add_header Cache-Control "public, immutable";
    }
    
    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
}
